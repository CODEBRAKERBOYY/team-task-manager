{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && CI=false npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s build --listen tcp://0.0.0.0:$PORT"
  }
}